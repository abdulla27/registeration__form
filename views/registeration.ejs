<link
  rel="stylesheet"
  href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css"
/>
<script src="./js/script.js"></script>

<section class="vh-100 gradient-custom">
  <div class="container py-5 h-100">
    <div class="row justify-content-center align-items-center h-100">
      <div class="col-12 col-lg-9 col-xl-7">
        <div
          class="card shadow-2-strong card-registration"
          style="border-radius: 15px"
        >
          <div class="card-body p-4 p-md-5">
            <h3 class="mb-4 pb-2 pb-md-0 mb-md-5">Registration Form</h3>
            <form method="post" action="/registeration">
              <div class="row">
                <div class="col-md-6 mb-4">
                  <div class="form-outline">
                    <input
                      type="text"
                      id="firstName"
                      class="form-control form-control-lg"
                      name="firstName"
                      placeholder="Enter First Name"
                      required
                    />
                    <label class="form-label" for="firstName">First Name</label>
                  </div>
                </div>
              <!-- </div> -->

              <!-- <div class="row"> -->
                <div class="col-md-6 mb-4">
                  <div class="form-outline">
                    <input
                      type="text"
                      id="lastName"
                      class="form-control form-control-lg"
                      name="lastName"
                      placeholder="Enter Last Name"
                      required
                    />
                    <label class="form-label" for="lastName">Last Name</label>
                  </div>
                </div>
                <div class="row">
                  <div class="col-md-6 mb-4 d-flex align-items-center">
                    <div class="form-outline datepicker w-100">
                      <input
                        type="date"
                        class="form-control form-control-lg"
                        name="dob"
                        placeholder="Enter DOB"
                        id="dob"
                        required
                      />
                     <div> <label for="dob" class="form-label">DOB</label><span style="margin-left: 10px; color: skyblue;" id="age"></span></div>
                      
                    </div>

                  </div>
                      
                    
                  </div>
  
              
              <div class="row">
                <div class="col-md-6 mb-4 d-flex align-items-center">
                  <div class="form-outline datepicker w-100">
                    <input
                      type="email"
                      class="form-control form-control-lg"
                      name="email"
                      placeholder="Enter Email"
                      id="email"
                      required
                    />
                    <label for="email" class="form-label">Email</label>
                  </div>
                </div>

                

            
                    <div class="col-md-3 mb-4 d-flex align-items-center">
                      <div class="form-outline datepicker w-100">
                        <select name="country" id="country" class="form-select form-select-lg mb-3" aria-label=".form-select-lg example"><option value="">country</option></select>
                    </div>
                    </div>
                        <div class="col-md-3 mb-4 d-flex align-items-center">
                          <div class="form-outline datepicker w-100">
                            <select name="state" id="state" class="form-select form-select-lg mb-3" aria-label=".form-select-lg example"><option value="">Select state</option></select>
                        
                        </div>
                        </div>
                        
                        <div class="row">
                        <div class="col-md-6 mb-4 d-flex align-items-center">
                          <div class="form-outline datepicker w-100">
                            <select name="city" id="city" class="form-select form-select-lg mb-3" aria-label=".form-select-lg example"><option value="">Select city</option></select>
                        
                        </div>

                        
                        </div>
                <div class="col-md-6 mb-4">
                  <h6 class="mb-2 pb-1">Gender:</h6>

                  <div class="form-check form-check-inline">
                    <input
                      class="form-check-input"
                      type="radio"
                      name="gender"
                      id="femaleGender"
                      value="female"
                    />
                    <label class="form-check-label" for="femaleGender"
                      >Female</label
                    >
                  </div>

                  <div class="form-check form-check-inline">
                    <input
                      class="form-check-input"
                      type="radio"
                      name="gender"
                      id="maleGender"
                      value="male"
                    />
                    <label class="form-check-label" for="maleGender"
                      >Male</label
                    >
                  </div>

                  <div class="form-check form-check-inline">
                    <input
                      class="form-check-input"
                      type="radio"
                      name="gender"
                      id="otherGender"
                      value="other"
                    />
                    <label class="form-check-label" for="otherGender"
                      >Other</label
                    >
                  </div>
                </div>
              </div>
              

        
              <div class="mt-4 pt-2">
                <center>
                  <input
                    class="btn btn-primary btn-lg"
                    type="submit"
                    value="Submit"
                  />
                </center>
              </div>

            </form>
          </div>
        </div>
      </div>
    </div>
    <script type="text/javascript">




        document.addEventListener('DOMContentLoaded',()=>{
    // const selectDrop=document.querySelector('#country');
    const selectDrop=document.getElementById('country')
    // console.log({selectDrop:selectDrop.innerHTML});
    fetch("https://countriesnow.space/api/v0.1/countries").then(res=>{
        return res.json();
    }).then(data=>{
        val=selectDrop.innerHTML;
        console.log(val);
        data.data.forEach(element => {
        // console.log(element);

            val+=`<option value="${element.country}">${element.country}</option>`
        });
        // val=JSON.parse(val)
        selectDrop.innerHTML=val
    }).catch(err=>{
        console.log(err);
    })

})




const selectElement = document.querySelector("#country");

selectElement.addEventListener("change", (event) => {
    console.log({selectElement});
  const result = document.querySelector("#state");
  const selectDrop=document.getElementById('country')
    // console.log({selectDrop});
     fetch("https://countriesnow.space/api/v0.1/countries/states",
{
    headers: {
      'Accept': 'application/json',
      'Content-Type': 'application/json'
    },
    method: "POST",
    body: JSON.stringify({country:selectElement.value})
}).then(res=>{
        return res.json();
    }).then(data=>{
        val=result.innerHTML;
        console.log(data);
        data.data.states.forEach(element => {
        // console.log(element);

            val=val+`<option value=${element.name}>${element.name}</option>`
        });
        // val=JSON.parse(val)
        result.innerHTML=val
    }).catch(err=>{
        console.log(err);
    })
  
  result.textContent = val;
});


const selectElement2 = document.querySelector("#state");

selectElement2.addEventListener("change", (event) => {
    // console.log({selectElement});
const city = document.querySelector("#city");

  const results = document.querySelector("#state");
  const selectDrop=document.getElementById('country')
    console.log({selectDrop:results.value});
     fetch("https://countriesnow.space/api/v0.1/countries/state/cities",
{
    headers: {
      'Accept': 'application/json',
      'Content-Type': 'application/json'
    },
    method: "POST",
    body: JSON.stringify({country:selectElement.value,state:results.value})
}).then(res=>{
        return res.json();
    }).then(data=>{
        val=city.innerHTML;
        console.log({data});
        data.data.forEach(element => {
        console.log(element);

            val=val+`<option value="${element}">${element}</option>`
        });
        // val=JSON.parse(val)
        city.innerHTML=val
    }).catch(err=>{
        console.log(err);
    })
  
  result.textContent = val;
});

function getAge(birthDate) {
  var now = new Date();

  function isLeap(year) {
    return year % 4 == 0 && (year % 100 != 0 || year % 400 == 0);
  }

  // days since the birthdate    
  var days = Math.floor((now.getTime() - birthDate.getTime())/1000/60/60/24);
  var age = 0;
  // iterate the years
  for (var y = birthDate.getFullYear(); y <= now.getFullYear(); y++){
    var daysInYear = isLeap(y) ? 366 : 365;
    if (days >= daysInYear){
      days -= daysInYear;
      age++;
      // increment the age only if there are available enough days for the year.
    }
  }
  return age}
const selectElement3 = document.querySelector("#dob");

selectElement3.addEventListener("change", (event) => {
    console.log({selectElement3:selectElement3.value});
    // let date=selectElement3.value.split("-")
    let newdate=new Date(selectElement3.value)
    
    let age=getAge(newdate)
    console.log(age,".......//.,,...");
    document.querySelector("#age").innerHTML=`your current age is ${age}`

    // let months={1:31,2:28,3:31,4:30,5:31,6:30,7:31,8:30,9:31,10:30,11:31,12:30}
})






    </script>
  </div>
</section>